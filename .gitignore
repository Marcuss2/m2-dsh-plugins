# Third-party research clones — large upstream repos fetched for analysis,
# not part of the reproduction kit. Keep only hand-written *.md notes there.
.research/oh-my-pi/
.research/dsh-dbg-inspect/

# Plugin working state (dsh-better-edit hash store)
.dsh_better_edit/

# Raw dshmarket Backup & Restore exports (browser downloads): they carry
# absolute home paths and whatever secrets the profile holds. Commit only the
# stripped canonical copy — see plugins/dshmarket/README.md.
dsh-dshmarket-backup-*.json

# Scratch files
test-hashline-edit.txt

# Generic noise
node_modules/
dist/
*.log
.env
.env.*
.DS_Store
.probe/
