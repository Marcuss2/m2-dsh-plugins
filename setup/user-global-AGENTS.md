# User-Global Agent Instructions

These instructions apply to **every session** on this DeepSeek Harness
install, regardless of workspace. Workspace-level `AGENTS.md` files may refine
them; where they conflict, the more specific instruction wins.

<!-- Keep this file minimal and machine-wide. Repository-specific directives
     belong in that repository's own AGENTS.md, not here. -->
